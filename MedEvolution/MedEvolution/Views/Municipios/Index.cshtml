@model IEnumerable<MedEvolution.Models.Municipio>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Departamento.NombreDep)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NombreMun)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Departamento.NombreDep)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NombreMun)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CodigoMunicipio }) |
            @Html.ActionLink("Details", "Details", new { id=item.CodigoMunicipio }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CodigoMunicipio })
        </td>
    </tr>
}

</table>
